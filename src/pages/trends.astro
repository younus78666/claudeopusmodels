---
import BaseLayout from "../layouts/BaseLayout.astro";
import SiloArchive from "../components/SiloArchive.astro";
import { silos, siteUrl } from "../content";

const silo = silos.find((s) => s.slug === "trends")!;
const schema = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  name: `${silo.label} | Claude Model Insights`,
  description: silo.description,
  url: `${siteUrl}${silo.href}`,
};
---

<BaseLayout
  title={`${silo.label} | AI Coding News, Predictions & Industry Shifts`}
  description={silo.description}
  path={silo.href}
  schema={schema}
>
  <SiloArchive silo={silo} accent="#B85C3A" />
</BaseLayout>
